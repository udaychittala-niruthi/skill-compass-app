// This component returns null because the actual splash screen
// and routing logic is handled by CustomSplashScreen component
// which is rendered as an overlay in _layout.tsx
export default function Index() {
    return null;
}
